// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.umbook.web;

import com.umbook.server.domain.Album;
import com.umbook.server.domain.ComentarioAlbum;
import com.umbook.server.domain.Foto;
import com.umbook.server.domain.Usuario;
import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.String;
import java.util.Collection;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect AlbumController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST)
    public String AlbumController.create(@Valid Album album, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("album", album);
            return "albums/create";
        }
        uiModel.asMap().clear();
        album.persist();
        return "redirect:/albums/" + encodeUrlPathSegment(album.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String AlbumController.createForm(Model uiModel) {
        uiModel.addAttribute("album", new Album());
        return "albums/create";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    public String AlbumController.show(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("album", Album.findAlbum(id));
        uiModel.addAttribute("itemId", id);
        return "albums/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String AlbumController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            uiModel.addAttribute("albums", Album.findAlbumEntries(page == null ? 0 : (page.intValue() - 1) * sizeNo, sizeNo));
            float nrOfPages = (float) Album.countAlbums() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("albums", Album.findAllAlbums());
        }
        return "albums/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String AlbumController.update(@Valid Album album, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("album", album);
            return "albums/update";
        }
        uiModel.asMap().clear();
        album.merge();
        return "redirect:/albums/" + encodeUrlPathSegment(album.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", method = RequestMethod.GET)
    public String AlbumController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("album", Album.findAlbum(id));
        return "albums/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE)
    public String AlbumController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        Album.findAlbum(id).remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/albums";
    }
    
    @ModelAttribute("albums")
    public Collection<Album> AlbumController.populateAlbums() {
        return Album.findAllAlbums();
    }
    
    @ModelAttribute("comentarioalbums")
    public Collection<ComentarioAlbum> AlbumController.populateComentarioAlbums() {
        return ComentarioAlbum.findAllComentarioAlbums();
    }
    
    @ModelAttribute("fotoes")
    public Collection<Foto> AlbumController.populateFotoes() {
        return Foto.findAllFotoes();
    }
    
    @ModelAttribute("usuarios")
    public Collection<Usuario> AlbumController.populateUsuarios() {
        return Usuario.findAllUsuarios();
    }
    
    String AlbumController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
